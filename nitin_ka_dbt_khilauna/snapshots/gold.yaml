snapshots:
    - name: gold
      relation: ref('gold_source')
      config:
        schema: gold
        database: '{{ target.catalog }}'
        unique_key: id
        strategy: timestamp
        updated_at: updateDate
        dbt_valid_to_current: NULL